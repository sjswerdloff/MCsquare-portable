[project]
name = "mcsquare-validation"
version = "0.1.0"
description = "Developer validation tools for MCsquare dose comparison"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pymedphys",
    "matplotlib",
]
